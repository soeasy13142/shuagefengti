# DH 密钥交换 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现 `pages/dh-viz/` 页面，用户设定公共参数（素数 p、本原根 g），模拟 Alice 和 Bob 双方从生成私钥 → 计算公钥 → 交换公钥 → 得到共享密钥的完整过程，并展示离散对数计算的难度和中间人攻击（MITM）模拟。

**Architecture:** 纯前端 + 2 个 utils 纯函数模块（dh-core / dh-mitm）+ 1 个页面（4 文件：js/wxml/wxss/json）。复用 rsa-core 的 modPow（提取到公共模块或直接复用）。Jest 全测。

**Tech Stack:** 微信小程序原生（WXML/WXSS/JS）+ Jest + CommonJS（require/module.exports）。

**Spec:** `docs/superpowers/specs/2026-07-19-dh-viz-design.md`

## Global Constraints

- 所有代码使用 `const`/`let`，**禁用 `var`**
- 文件命名：小写 + kebab-case
- 私有函数：`_underscore` 前缀
- 异步优先：`Promise` / `async-await`
- 错误处理：所有 `catch` 必须显式处理或 `throw`
- 注释：`/** */` JSDoc 用于公开 API；行内 `//` 仅用于 why
- 测试命令：`cd /Users/charliepan/Downloads/my-miniapp && npm test`
- 设计风格：Claude Design 暖奶油画布（`#faf9f5` / `#efe9de` / `#cc785c` / Georgia）
- 不引入第三方依赖
- 中文 UI 文案；变量名/函数名/注释/commit 英文
- **复用 modPow**：导入 `utils/rsa-core.js` 的 modPow（或提取到公共 `utils/modular-arithmetic.js`）
- **本原根判定**：对素数 p，遍历 g^(p-1)/q ≠ 1 对 p-1 的每个素因子 q
- **数字限制**：p ≤ 997（与 RSA 一致）

---

## Task 1: DH 核心实现（本原根 + 密钥交换 + 离散对数暴力搜索）

**Files:**
- Create: `utils/dh-core.js`
- Create: `utils/dh-mitm.js`
- Test: `tests/utils/dh-core.test.js`

**Interfaces:**

`utils/dh-core.js`:
- `isPrimitiveRoot(g: number, p: number)`: `boolean` — 判定 g 是否为模 p 的本原根
- `findPrimitiveRoots(p: number)`: `number[]` — 找出模 p 的所有本原根
- `generateKeypair(p: number, g: number, privateKey?: number)`: `{ privateKey: number, publicKey: number }` — 生成 DH 密钥对
- `computeSharedKey(theirPublic: number, myPrivate: number, p: number)`: `number` — 计算共享密钥
- `bruteForceDiscreteLog(g: number, p: number, target: number)`: `{ foundX: number | null, attempts: number, totalSpace: number, steps: Array<{ x, result }> }` — 暴力搜索离散对数

`utils/dh-mitm.js`:
- `generateMitmScenario(p: number, g: number, alicePriv: number, bobPriv: number, evePriv: number)`: `{ alice, bob, eve, steps: Array<{ step, from, to, type, payload, mitm? }> }` — MITM 攻击场景
  - Alice 发公钥 A → Eve 拦截 → Eve 替换为 E_a（Eve 的公钥）→ 转发给 Bob
  - Bob 发公钥 B → Eve 拦截 → Eve 替换为 E_b → 转发给 Alice
  - Alice 计算 K_ae = E_b^a mod p
  - Bob 计算 K_be = E_a^b mod p
  - Eve 计算 K_ae = A^e mod p 和 K_be = B^e mod p

**Test vectors (must pass):**

```js
// isPrimitiveRoot 已知结果:
// p=13, g=2 → true (2^12 ≡ 1, 但 2^6≠1, 2^4≠1, 2^3≠1, 2^2≠1)
// p=13, g=3 → false (3^3 ≡ 1 mod 13)
// p=13, g=6 → false
// p=13, g=7 → false

// findPrimitiveRoots(13) → [2, 6, 7, 11] (模 13 的本原根有 4 个)

// DH 完整交换:
// p=997, g=7, a=123, b=456
// Alice 公钥: 7^123 mod 997, Bob 公钥: 7^456 mod 997
// 共享密钥一致: Alice 的 B^a mod p === Bob 的 A^b mod p

// 离散对数暴力搜索:
// g=7, p=997, target=312 → 搜索找到 x=123
```

- [ ] **Step 1: Write failing test**

`tests/utils/dh-core.test.js`:

```js
const { modPow } = require('../../utils/rsa-core');
const { isPrimitiveRoot, findPrimitiveRoots, generateKeypair, computeSharedKey, bruteForceDiscreteLog } = require('../../utils/dh-core');
const { generateMitmScenario } = require('../../utils/dh-mitm');

describe('isPrimitiveRoot', () => {
  test('2 is primitive root modulo 13', () => {
    expect(isPrimitiveRoot(2, 13)).toBe(true);
  });
  test('3 is NOT primitive root modulo 13', () => {
    expect(isPrimitiveRoot(3, 13)).toBe(false);
  });
  test('6 is NOT primitive root modulo 13', () => {
    expect(isPrimitiveRoot(6, 13)).toBe(false);
  });
  test('7 is NOT primitive root modulo 13', () => {
    expect(isPrimitiveRoot(7, 13)).toBe(false);
  });
  test('g must be > 1 and < p', () => {
    expect(isPrimitiveRoot(1, 13)).toBe(false);
    expect(isPrimitiveRoot(13, 13)).toBe(false);
  });
});

describe('findPrimitiveRoots', () => {
  test('p=13 has 4 primitive roots', () => {
    const roots = findPrimitiveRoots(13);
    expect(roots.length).toBe(4);
    expect(roots).toEqual(expect.arrayContaining([2, 6, 7, 11]));
  });
  test('p=7 has 2 primitive roots: 3, 5', () => {
    const roots = findPrimitiveRoots(7);
    expect(roots).toEqual(expect.arrayContaining([3, 5]));
    expect(roots.length).toBe(2);
  });
  test('p=5 has 2 primitive roots: 2, 3', () => {
    const roots = findPrimitiveRoots(5);
    expect(roots).toEqual(expect.arrayContaining([2, 3]));
  });
});

describe('generateKeypair', () => {
  test('generates correct public key from given private key', () => {
    const key = generateKeypair(997, 7, 123);
    expect(key.privateKey).toBe(123);
    expect(key.publicKey).toBe(modPow(7, 123, 997));
  });
  test('generates random private key if not specified', () => {
    const key = generateKeypair(997, 7);
    expect(key.privateKey).toBeGreaterThan(1);
    expect(key.privateKey).toBeLessThan(996);
    expect(key.publicKey).toBeGreaterThan(0);
  });
  test('private key must be > 1', () => {
    expect(() => generateKeypair(997, 7, 0)).toThrow();
    expect(() => generateKeypair(997, 7, 1)).toThrow();
  });
  test('private key must be < p', () => {
    expect(() => generateKeypair(997, 7, 997)).toThrow();
  });
});

describe('computeSharedKey', () => {
  test('Alice and Bob compute same shared key', () => {
    const p = 997, g = 7;
    const alice = generateKeypair(p, g, 123);
    const bob = generateKeypair(p, g, 456);
    const sharedA = computeSharedKey(bob.publicKey, alice.privateKey, p);
    const sharedB = computeSharedKey(alice.publicKey, bob.privateKey, p);
    expect(sharedA).toBe(sharedB);
  });
  test('multiple key pairs produce consistent shared keys', () => {
    const p = 991, g = 6; // 6 is a primitive root of 991
    const alice = generateKeypair(p, g, 234);
    const bob = generateKeypair(p, g, 567);
    expect(computeSharedKey(bob.publicKey, alice.privateKey, p))
      .toBe(computeSharedKey(alice.publicKey, bob.privateKey, p));
  });
});

describe('bruteForceDiscreteLog', () => {
  test('finds known discrete log', () => {
    const result = bruteForceDiscreteLog(7, 997, 312);
    expect(result.foundX).toBe(123);
    expect(result.attempts).toBe(123);
    expect(result.totalSpace).toBe(996);
  });
  test('returns null when not found', () => {
    const result = bruteForceDiscreteLog(7, 997, 999); // 999 > 996 possible results
    expect(result.foundX).toBeNull();
  });
  test('generates steps array', () => {
    const result = bruteForceDiscreteLog(7, 23, 17);
    expect(result.steps.length).toBeGreaterThan(0);
    expect(result.steps[0].x).toBe(1);
    expect(result.steps[0].result).toBeDefined();
  });
});

describe('generateMitmScenario', () => {
  test('produces 3 participants', () => {
    const scenario = generateMitmScenario(997, 7, 123, 456, 789);
    expect(scenario.alice).toBeDefined();
    expect(scenario.bob).toBeDefined();
    expect(scenario.eve).toBeDefined();
    expect(scenario.alice.name).toBe('Alice');
    expect(scenario.eve.name).toBe('Eve');
  });
  test('Alice and Bob have different shared keys under MITM', () => {
    const scenario = generateMitmScenario(997, 7, 123, 456, 789);
    // Under MITM, Alice shares key with Eve, Bob shares key with Eve
    // Alice and Bob should NOT have the same key
    expect(scenario.alice.sharedKey).not.toBe(scenario.bob.sharedKey);
    // Alice and Eve share the same key
    expect(scenario.alice.sharedKey).toBe(scenario.eve.sharedKeyAlice);
    // Bob and Eve share the same key
    expect(scenario.bob.sharedKey).toBe(scenario.eve.sharedKeyBob);
  });
  test('mitm steps include intercept messages', () => {
    const scenario = generateMitmScenario(997, 7, 123, 456, 789);
    const mitmSteps = scenario.steps.filter(s => s.type === 'mitmIntercept');
    expect(mitmSteps.length).toBe(2); // Intercept A and Intercept B
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd /Users/charliepan/Downloads/my-miniapp && npx jest tests/utils/dh-core.test.js`
Expected: FAIL with "Cannot find module"

- [ ] **Step 3: Implement `utils/dh-core.js`**

包含：
- `isPrimitiveRoot(g, p)`: 对 p-1 的素因子 q 遍历，检查 g^(p-1)/q ≠ 1 mod p
- `findPrimitiveRoots(p)`: 枚举 [2, p-2] 范围内的 g，用 isPrimitiveRoot 筛选
- `generateKeypair(p, g, privateKey?)`: 若 privateKey 不传则随机生成 [2, p-2]
- `computeSharedKey(theirPublic, myPrivate, p)`: modPow(theirPublic, myPrivate, p)
- `bruteForceDiscreteLog(g, p, target)`: 从 1 到 p-1 遍历 g^x mod p，匹配 target

注意：从 `utils/rsa-core.js` 导入 `modPow` 使用。

- [ ] **Step 4: Implement `utils/dh-mitm.js`**

包含：
- `generateMitmScenario(p, g, alicePriv, bobPriv, evePriv)`: 完整的 MITM 场景生成
- 返回所有参与者的密钥状态和步骤数组

- [ ] **Step 5: Run test to verify it passes**

Run: `cd /Users/charliepan/Downloads/my-miniapp && npx jest tests/utils/dh-core.test.js`
Expected: PASS

- [ ] **Step 6: Run full test suite**

Run: `cd /Users/charliepan/Downloads/my-miniapp && npm test`
Expected: ALL PASS

- [ ] **Step 7: Commit**

```bash
git add utils/dh-core.js utils/dh-mitm.js tests/utils/dh-core.test.js
git commit -m "feat(dh-core): DH 核心（本原根判定/密钥交换/离散对数暴力搜索/MITM）"
```

---

## Task 2: DH 演示页面（4 文件）

**Files:**
- Create: `pages/dh-viz/dh-viz.json`
- Create: `pages/dh-viz/dh-viz.wxml`
- Create: `pages/dh-viz/dh-viz.wxss`
- Create: `pages/dh-viz/dh-viz.js`

**Interfaces:**
- Consumes:
  - `isPrimitiveRoot`, `findPrimitiveRoots`, `generateKeypair`, `computeSharedKey`, `bruteForceDiscreteLog` from `utils/dh-core.js`
  - `generateMitmScenario` from `utils/dh-mitm.js`
  - `modPowWithSteps` from `utils/rsa-core.js`（可选，展示模幂过程）
- Produces: 完整的 DH 演示页面

**页面结构：**

1. **顶部参数区**：
   - 素数 p 下拉选择器 + 本原根 g 自动计算列表
   - 场景切换（正常交换 / MITM 攻击）
   - 开始/重置/暂停按钮

2. **主视图 — 双方面板**（Alice | Bob）：
   - 私钥输入（手动或随机）
   - 公钥计算展示（模幂过程展开）
   - 收到对方公钥展示
   - 共享密钥计算展示
   - 密钥一致性标识
   - MITM 场景：中间出现 Eve 面板

3. **步进动画 — 连接线**：
   - Alice → Bob 公钥交换箭头动画
   - MITM 场景红色虚线箭头
   - 步骤文案说明

4. **离散对数演示面板**（可折叠）：
   - 暴力搜索过程展示
   - 进度条
   - 搜索空间提示

- [ ] **Step 1: Write `pages/dh-viz/dh-viz.json`**

```json
{
  "navigationBarTitleText": "DH 密钥交换",
  "navigationBarBackgroundColor": "#faf9f5",
  "navigationBarTextStyle": "black",
  "backgroundColor": "#faf9f5",
  "usingComponents": {}
}
```

- [ ] **Step 2: Write `pages/dh-viz/dh-viz.wxml`**

布局：
1. 参数区卡片：素数选择器 → 自动列出本原根 → 场景模式切换
2. 双方面板：Alice 面板和 Bob 面板左右并排
3. MITM 场景下中间弹出 Eve 面板
4. 步进控制器：前进/后退/暂停 + 步骤说明
5. 离散对数面板（可折叠）
6. 密钥验证结果展示

- [ ] **Step 3: Write `pages/dh-viz/dh-viz.wxss`**

Claude Design 暖奶油画布风格：
- Alice/Bob 双栏布局（flex）
- 面板间连接箭头（CSS 伪元素实现方向指示）
- MITM 红色虚线样式
- 密钥一致性标识（绿色 ✅ / 红色 ⚠️）
- 进度条样式（离散对数搜索进度）
- 模幂过程表格/步骤展示

- [ ] **Step 4: Write `pages/dh-viz/dh-viz.js`**

页面逻辑：
- `data`：p, g, 场景模式, Alice 状态, Bob 状态, Eve 状态（MITM）, 步骤数组, 当前步骤, 离散对数结果
- 素数选择：p 改变时自动计算本原根列表
- 密钥生成：GenerateKeypair 用指定或随机私钥
- 场景切换：正常 → 自动交换公钥；MITM → Eve 拦截展示
- 步进控制：逐步展示公钥交换流程
- 离散对数：在底部面板展示暴力搜索过程
- 自动播放：可配置速度

- [ ] **Step 5: Run full test suite**

Run: `cd /Users/charliepan/Downloads/my-miniapp && npm test`
Expected: ALL PASS

- [ ] **Step 6: Commit**

```bash
git add pages/dh-viz/
git commit -m "feat(dh-viz): 页面（参数/双方面板/步进动画/MITM/离散对数演示）"
```

---

## Task 3: 注册页面 + tool-registry + 模块文档

**Files:**
- Modify: `app.json`（添加 `pages/dh-viz/dh-viz` 到主 pages 数组）
- Modify: `utils/tool-registry.js`（把 `dh-viz.available` 改为 `true`，补充字段）
- Create: `docs/handoff/modules/dh-viz.md`

- [ ] **Step 1: Modify `app.json`**

在主 pages 数组中追加 `"pages/dh-viz/dh-viz"`。

- [ ] **Step 2: Modify `utils/tool-registry.js`**

找到密码学分类下 `dh-viz` 工具对象，`available: false` → `true`，补充字段。

```js
tagline: 'Alice 和 Bob 怎么在不安全信道上约定密钥？DH 密钥交换一步一步看',
taglineDetail: '设定素数和本原根，Alice 和 Bob 自动生成私钥→交换公钥→计算共享密钥；切换 MITM 模式看 Eve 怎么拦截篡改',
tags: ['#可视化', '#进阶'],
difficulty: 'advanced',
intro: {
  valueProp: '选一个素数和一个本原根，看 Alice 和 Bob 怎么在不安全信道上约定共享密钥。',
  features: [
    'Alice/Bob 双方面板，私钥可手输可随机，公钥自动计算',
    '公钥交换过程动画展示（含模幂展开）',
    'MITM 攻击模拟：Eve 拦截篡改，密钥不一致警示'
  ],
  prerequisites: '知道"密钥交换"的概念，了解模幂运算基本知识。',
  useCases: [
    '密码学课程密钥交换章节',
    '理解 DH 协议如何防止窃听',
    '演示中间人攻击的工作原理和危害'
  ]
}
```

- [ ] **Step 3: Write `docs/handoff/modules/dh-viz.md`**

```md
# DH 密钥交换（Diffie-Hellman Key Exchange）

> 由 `2026-07-19-dh-viz-design.md` spec 实施落盘。

## 概览

Diffie-Hellman 密钥交换协议的可视化教学页面：设定公共参数（素数 p、本原根 g），模拟 Alice 和 Bob 从生成私钥到共享密钥的完整过程，并展示离散对数搜索难度和中间人攻击。

## 数据驱动

- `utils/dh-core.js`：本原根判定 / 密钥对生成 / 共享密钥计算 / 离散对数暴力搜索
- `utils/dh-mitm.js`：MITM 攻击场景模拟
- 复用 `utils/rsa-core.js` 的 modPow

## 特性

| 特性 | 说明 |
|---|---|
| 公共参数 | 素数 p 下拉 + 本原根 g 自动计算列表 |
| 双方面板 | Alice/Bob 私钥、公钥、共享密钥完整计算过程 |
| MITM 模拟 | Eve 拦截公钥并替换，展示密钥不一致 |
| 离散对数 | 暴力搜索 g^x ≡ A 的过程动画 + 进条度 |
| 步进控制 | 前/后/自动播放，公钥交换过程动画 |
```

- [ ] **Step 4: Run full test suite**

Run: `cd /Users/charliepan/Downloads/my-miniapp && npm test`
Expected: ALL PASS

- [ ] **Step 5: Commit**

```bash
git add app.json utils/tool-registry.js docs/handoff/modules/dh-viz.md
git commit -m "feat(dh-viz): 注册页面 + tool-registry 可用化 + 模块文档"
```

---

## 验收清单（实施完成后）

- [ ] `npm test` 全绿
- [ ] `isPrimitiveRoot(2, 13)` → true, `isPrimitiveRoot(3, 13)` → false
- [ ] `findPrimitiveRoots(13)` → [2, 6, 7, 11]
- [ ] DH 完整交换：Alice 和 Bob 共享密钥一致
- [ ] 离散对数暴力搜索找到已知值
- [ ] MITM 场景产生不同的 Alice/Bob 共享密钥
- [ ] 页面加载无报错
- [ ] 素数选择后自动列出本原根
- [ ] Alice/Bob 双方面板展示正确
- [ ] MITM 场景下中间人面板正常显示
- [ ] 离散对数面板进度条展示
- [ ] 首页「密码学」分类 → DH 密钥交换卡片可点击
