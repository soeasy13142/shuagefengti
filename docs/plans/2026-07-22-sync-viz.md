# 同步互斥演示（生产者-消费者 + 信号量 PV 操作）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现 `pages/sync-viz/` 页面，可视化生产者-消费者问题中信号量（P/V 操作）控制进程同步的过程，含缓冲区状态展示、信号量数值实时变化、步骤动画、操作日志和播放控制。

**Architecture:** 纯前端 + 2 个 utils 纯函数模块（semaphore / producer-consumer）+ 1 个页面（4 文件：js/wxml/wxss/json）。WXML/WXSS 实现可视化，不引第三方库。Jest 全测。

**Tech Stack:** 微信小程序原生（WXML/WXSS/JS）+ Jest + CommonJS（require/module.exports）。

**Spec:** `docs/superpowers/specs/2026-07-19-sync-viz-design.md`

## Global Constraints

- 所有代码使用 `const`/`let`，**禁用 `var`**（CLAUDE.md 代码风格）
- 文件命名：小写 + kebab-case
- 私有函数 / 模块状态：`_underscore` 前缀
- 异步优先：`Promise` / `async-await`
- 错误处理：所有 `catch` 必须显式处理或 `throw`，禁止静默吞
- 注释：`/** */` JSDoc 用于公开 API；行内 `//` 仅用于解释 why
- 测试命令：`cd /Users/charliepan/Downloads/my-miniapp && npm test`（必须全绿）
- 设计风格：Claude Design 暖奶油画布（背景 `#faf9f5`、卡片 `#efe9de`、CTA `#cc785c`、Georgia 标题）
- 不引入第三方依赖（无新 npm 包）
- 中文 UI 文案；变量名 / 函数名 / 注释 / commit 英文

---

## Task 1: 信号量模块

**Files:**
- Create: `utils/semaphore.js`
- Test: `tests/utils/semaphore.test.js`

**Interfaces:**
- Consumes: 无（独立模块）
- Produces:
  - `createSemaphore(initialValue)` : `→ Semaphore`（不可变创建）
  - `semWait(semaphore, ownerId)` : `→ { semaphore: Semaphore, blocked: boolean }`（P 操作）
  - `semSignal(semaphore)` : `→ { semaphore: Semaphore, woken: string|null }`（V 操作）
  - `Semaphore` 数据结构: `{ value: number, initial: number, queue: string[], history: OperationRecord[] }`

`Semaphore` 数据结构：

```js
{
  value: number,        // 当前值（可负，负值绝对值 = 等待队列长度）
  initial: number,      // 初始值
  queue: string[],      // 等待队列（ownerId 列表）
  history: OperationRecord[]  // 变更记录
}
```

`OperationRecord`:

```js
{
  type: 'P' | 'V',
  ownerId: string,
  valueBefore: number,
  valueAfter: number,
  blocked: boolean,
  woken: string | null,
  timestamp: number
}
```

- [ ] **Step 1: Write failing test**

`tests/utils/semaphore.test.js`:

```js
const { createSemaphore, semWait, semSignal } = require('../../utils/semaphore');

describe('createSemaphore', () => {
  test('creates with given initial value', () => {
    const s = createSemaphore(1);
    expect(s.value).toBe(1);
    expect(s.initial).toBe(1);
    expect(s.queue).toEqual([]);
    expect(s.history).toEqual([]);
  });

  test('creates with zero value', () => {
    const s = createSemaphore(0);
    expect(s.value).toBe(0);
  });

  test('creates with negative value (blocked state)', () => {
    const s = createSemaphore(-2);
    expect(s.value).toBe(-2);
  });

  test('throws for non-integer value', () => {
    expect(() => createSemaphore(1.5)).toThrow(/integer/i);
  });
});

describe('semWait (P operation)', () => {
  test('P on positive semaphore: decrements value, not blocked', () => {
    const s = createSemaphore(1);
    const { semaphore, blocked } = semWait(s, 'P1');
    expect(semaphore.value).toBe(0);
    expect(blocked).toBe(false);
    expect(semaphore.queue).toEqual([]);
  });

  test('P on zero semaphore: value becomes -1, owner blocked and added to queue', () => {
    const s = createSemaphore(0);
    const { semaphore, blocked } = semWait(s, 'P1');
    expect(semaphore.value).toBe(-1);
    expect(blocked).toBe(true);
    expect(semaphore.queue).toEqual(['P1']);
  });

  test('P on negative semaphore: value decreases, owner blocked', () => {
    const s = { value: -1, initial: 0, queue: ['P0'], history: [] };
    const { semaphore, blocked } = semWait(s, 'P2');
    expect(semaphore.value).toBe(-2);
    expect(blocked).toBe(true);
    expect(semaphore.queue).toEqual(['P0', 'P2']);
  });

  test('P records history entry', () => {
    const s = createSemaphore(2);
    const { semaphore } = semWait(s, 'P1');
    expect(semaphore.history).toHaveLength(1);
    expect(semaphore.history[0].type).toBe('P');
    expect(semaphore.history[0].ownerId).toBe('P1');
    expect(semaphore.history[0].valueBefore).toBe(2);
    expect(semaphore.history[0].valueAfter).toBe(1);
    expect(semaphore.history[0].blocked).toBe(false);
  });

  test('P preserves immutability (original semaphore unchanged)', () => {
    const s = createSemaphore(1);
    const originalValue = s.value;
    semWait(s, 'P1');
    expect(s.value).toBe(originalValue);
  });
});

describe('semSignal (V operation)', () => {
  test('V on positive semaphore: increments value, no wake', () => {
    const s = createSemaphore(0);
    const { semaphore, woken } = semSignal(s);
    expect(semaphore.value).toBe(1);
    expect(woken).toBeNull();
    expect(semaphore.queue).toEqual([]);
  });

  test('V with waiting process: increments value, wakes the first in queue', () => {
    const s = { value: -1, initial: 0, queue: ['P1'], history: [] };
    const { semaphore, woken } = semSignal(s);
    expect(semaphore.value).toBe(0);
    expect(woken).toBe('P1');
    expect(semaphore.queue).toEqual([]);
  });

  test('V wakes processes in FIFO order', () => {
    const s = { value: -2, initial: 0, queue: ['P1', 'P2'], history: [] };
    const first = semSignal(s);
    expect(first.woken).toBe('P1');
    expect(first.semaphore.queue).toEqual(['P2']);
    expect(first.semaphore.value).toBe(-1);

    const second = semSignal(first.semaphore);
    expect(second.woken).toBe('P2');
    expect(second.semaphore.queue).toEqual([]);
    expect(second.semaphore.value).toBe(0);
  });

  test('V with queue empty but value 0: increments, no wake', () => {
    const s = createSemaphore(0);
    const { semaphore, woken } = semSignal(s);
    expect(semaphore.value).toBe(1);
    expect(woken).toBeNull();
  });

  test('V records history entry', () => {
    const s = createSemaphore(0);
    const { semaphore } = semSignal(s);
    expect(semaphore.history).toHaveLength(1);
    expect(semaphore.history[0].type).toBe('V');
    expect(semaphore.history[0].valueBefore).toBe(0);
    expect(semaphore.history[0].valueAfter).toBe(1);
  });

  test('V preserves immutability (original semaphore unchanged)', () => {
    const s = createSemaphore(0);
    const originalValue = s.value;
    semSignal(s);
    expect(s.value).toBe(originalValue);
  });
});

describe('P and V interaction', () => {
  test('P then V returns to original state', () => {
    const s = createSemaphore(1);
    const afterP = semWait(s, 'P1');
    expect(afterP.semaphore.value).toBe(0);
    const afterV = semSignal(afterP.semaphore);
    expect(afterV.semaphore.value).toBe(1);
  });

  test('two Ps then two Vs: value returns to initial', () => {
    const s = createSemaphore(2);
    const r1 = semWait(s, 'P1');
    const r2 = semWait(r1.semaphore, 'P2');
    expect(r2.semaphore.value).toBe(0);

    const r3 = semSignal(r2.semaphore);
    expect(r3.semaphore.value).toBe(1);
    const r4 = semSignal(r3.semaphore);
    expect(r4.semaphore.value).toBe(2);
  });

  test('blocking and wake cycle: P on 0 blocks, V wakes', () => {
    const s = createSemaphore(0);
    const pResult = semWait(s, 'P1');
    expect(pResult.blocked).toBe(true);

    const vResult = semSignal(pResult.semaphore);
    expect(vResult.woken).toBe('P1');
    expect(vResult.semaphore.value).toBe(0);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd /Users/charliepan/Downloads/my-miniapp && npx jest tests/utils/semaphore.test.js`
Expected: FAIL with "Cannot find module '../../utils/semaphore'"

- [ ] **Step 3: Implement `utils/semaphore.js`**

```js
/**
 * 信号量模块（纯函数，不可变操作）
 *
 * 实现 PV 信号量操作（Dijkstra 的 P/V 原语）：
 * - P 操作（semWait）：申请资源，value--，若 value<0 则阻塞
 * - V 操作（semSignal）：释放资源，value++，若 value<=0 则唤醒一个等待者
 *
 * 所有函数返回新状态，不修改入参。
 */

/**
 * 创建信号量
 * @param {number} initialValue 初始值
 * @returns {{ value: number, initial: number, queue: string[], history: Array }}
 */
function createSemaphore(initialValue) {
  if (typeof initialValue !== 'number' || !Number.isInteger(initialValue)) {
    throw new Error('createSemaphore: initialValue must be an integer');
  }
  return {
    value: initialValue,
    initial: initialValue,
    queue: [],
    history: []
  };
}

/**
 * P 操作（wait）：申请资源
 * @param {{ value: number, initial: number, queue: string[], history: Array }} semaphore
 * @param {string} ownerId 请求资源的进程/线程 ID
 * @returns {{ semaphore: { value: number, initial: number, queue: string[], history: Array }, blocked: boolean }}
 */
function semWait(semaphore, ownerId) {
  const newValue = semaphore.value - 1;
  const blocked = newValue < 0;
  const newQueue = blocked ? semaphore.queue.concat([ownerId]) : semaphore.queue.slice();
  const record = {
    type: 'P',
    ownerId,
    valueBefore: semaphore.value,
    valueAfter: newValue,
    blocked,
    woken: null,
    timestamp: Date.now()
  };
  return {
    semaphore: {
      value: newValue,
      initial: semaphore.initial,
      queue: newQueue,
      history: semaphore.history.concat([record])
    },
    blocked
  };
}

/**
 * V 操作（signal）：释放资源
 * @param {{ value: number, initial: number, queue: string[], history: Array }} semaphore
 * @returns {{ semaphore: { value: number, initial: number, queue: string[], history: Array }, woken: string|null }}
 */
function semSignal(semaphore) {
  const newValue = semaphore.value + 1;
  let woken = null;
  let newQueue = semaphore.queue.slice();
  if (newValue <= 0 && newQueue.length > 0) {
    woken = newQueue[0];
    newQueue = newQueue.slice(1);
  }
  const record = {
    type: 'V',
    ownerId: woken || '',
    valueBefore: semaphore.value,
    valueAfter: newValue,
    blocked: false,
    woken,
    timestamp: Date.now()
  };
  return {
    semaphore: {
      value: newValue,
      initial: semaphore.initial,
      queue: newQueue,
      history: semaphore.history.concat([record])
    },
    woken
  };
}

module.exports = {
  createSemaphore,
  semWait,
  semSignal
};
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd /Users/charliepan/Downloads/my-miniapp && npx jest tests/utils/semaphore.test.js`
Expected: PASS (~25 tests)

- [ ] **Step 5: Commit**

```bash
git add utils/semaphore.js tests/utils/semaphore.test.js
git commit -m "feat(semaphore): 信号量 PV 操作实现（不可变 + FIFO 等待队列 + history）"
```

---

## Task 2: 生产者-消费者模拟引擎

**Files:**
- Create: `utils/producer-consumer.js`
- Test: `tests/utils/producer-consumer.test.js`

**Interfaces:**
- Consumes: `semWait`, `semSignal`, `createSemaphore` from `utils/semaphore.js`
- Produces:
  - `createSimulation(bufferSize)` : `→ SimulationState`
  - `producerStep(state, produceItem)` : `→ { state: SimulationState, logs: LogEntry[], blocked: boolean }`
  - `consumerStep(state)` : `→ { state: SimulationState, logs: LogEntry[], blocked: boolean }`
  - `addRandomItems(state, count?)` : `→ { state: SimulationState, logs: LogEntry[] }`（批量添加生产者日志）
  - `resetSimulation(state)` : `→ SimulationState`

`SimulationState` 数据结构：

```js
{
  buffer: Array(string|null),    // 环形缓冲区，null = 空槽
  bufferSize: number,            // 缓冲区容量
  producerIndex: number,         // 生产者写指针
  consumerIndex: number,         // 消费者读指针
  semaphores: {
    mutex: Semaphore,            // 互斥信号量
    empty: Semaphore,            // 空闲槽信号量
    full: Semaphore              // 已满槽信号量
  },
  steps: LogEntry[],             // 完整操作历史
  stepCount: number              // 已执行步数
}
```

`LogEntry` 数据结构：

```js
{
  id: number,                    // 步骤 ID（全局单调）
  timestamp: number,             // 模拟时间（stepCount）
  actor: 'producer' | 'consumer',
  action: string,                // 操作描述，如 "P(mutex)" / "produce 🍎" / "V(empty)"
  semaphoreBefore?: number,      // 操作前信号量值
  semaphoreAfter?: number,       // 操作后信号量值
  semaphoreName?: string,        // 'mutex' | 'empty' | 'full'
  blocked: boolean,              // 是否因阻塞而等待
  woken: boolean,                // 是否刚被唤醒
  item?: string                  // 生产/消费的物品标识
}
```

- [ ] **Step 1: Write failing test**

`tests/utils/producer-consumer.test.js`:

```js
const {
  createSimulation,
  producerStep,
  consumerStep,
  addRandomItems,
  resetSimulation
} = require('../../utils/producer-consumer');

describe('createSimulation', () => {
  test('creates simulation with correct buffer size', () => {
    const sim = createSimulation(4);
    expect(sim.buffer).toHaveLength(4);
    expect(sim.bufferSize).toBe(4);
    expect(sim.buffer.every(slot => slot === null)).toBe(true);
    expect(sim.producerIndex).toBe(0);
    expect(sim.consumerIndex).toBe(0);
    expect(sim.semaphores.empty.value).toBe(4);
    expect(sim.semaphores.full.value).toBe(0);
    expect(sim.semaphores.mutex.value).toBe(1);
    expect(sim.steps).toEqual([]);
    expect(sim.stepCount).toBe(0);
  });

  test('creates simulation with size 2 (minimum)', () => {
    const sim = createSimulation(2);
    expect(sim.buffer).toHaveLength(2);
  });

  test('creates simulation with size 8 (maximum)', () => {
    const sim = createSimulation(8);
    expect(sim.buffer).toHaveLength(8);
  });

  test('clamps bufferSize < 2 to 2', () => {
    const sim = createSimulation(1);
    expect(sim.buffer).toHaveLength(2);
  });

  test('clamps bufferSize > 8 to 8', () => {
    const sim = createSimulation(10);
    expect(sim.buffer).toHaveLength(8);
  });
});

describe('producerStep', () => {
  test('single produce: places item, decrements empty, increments full', () => {
    const sim = createSimulation(4);
    const result = producerStep(sim, '🍎');
    expect(result.blocked).toBe(false);
    expect(result.state.buffer[0]).toBe('🍎');
    expect(result.state.producerIndex).toBe(1);
    expect(result.state.semaphores.empty.value).toBe(3);
    expect(result.state.semaphores.full.value).toBe(1);
    expect(result.state.semaphores.mutex.value).toBe(1);
    expect(result.state.stepCount).toBe(1);
  });

  test('produce fills buffer to capacity', () => {
    let sim = createSimulation(3);
    for (let i = 0; i < 3; i++) {
      const r = producerStep(sim, 'item' + i);
      expect(r.blocked).toBe(false);
      sim = r.state;
    }
    expect(sim.buffer.filter(s => s !== null)).toHaveLength(3);
    expect(sim.semaphores.empty.value).toBe(0);
    expect(sim.semaphores.full.value).toBe(3);

    // 第 4 次生产阻塞（缓冲区满）
    const blocked = producerStep(sim, 'overflow');
    expect(blocked.blocked).toBe(true);
    // 状态不变
    expect(blocked.state.semaphores.empty.value).toBe(0);
  });

  test('produce generates log entries for each operation', () => {
    const sim = createSimulation(3);
    const result = producerStep(sim, '🍊');
    // 4 条日志：P(empty) / P(mutex) / produce / V(mutex) / V(full)
    // 实际上 producerStep 内部做了完整的 PV 序列
    expect(result.state.steps.length).toBeGreaterThan(0);
    // 检查日志包含操作类型
    const stepTypes = result.state.steps.map(s => s.action);
    expect(stepTypes.some(a => a.includes('P(empty)'))).toBe(true);
    expect(stepTypes.some(a => a.includes('V(full)'))).toBe(true);
  });

  test('produce wraps around buffer (circular)', () => {
    const sim = createSimulation(3);
    // 从 slot 0 写到 slot 2（共 3 个），然后 wrap 回 slot 0
    let s = sim;
    for (let i = 0; i < 3; i++) {
      const r = producerStep(s, 'x');
      s = r.state;
    }
    expect(s.producerIndex).toBe(0); // wrapped
  });
});

describe('consumerStep', () => {
  test('cannot consume from empty buffer (blocks)', () => {
    const sim = createSimulation(3);
    const result = consumerStep(sim);
    expect(result.blocked).toBe(true);
    // buffer unchanged
    expect(result.state.buffer.every(s => s === null)).toBe(true);
  });

  test('consumes after produce', () => {
    let sim = createSimulation(3);
    const produced = producerStep(sim, '🍇');
    sim = produced.state;

    const consumed = consumerStep(sim);
    expect(consumed.blocked).toBe(false);
    expect(consumed.state.buffer[0]).toBe(null); // slot freed
    expect(consumed.state.consumerIndex).toBe(1);
    expect(consumed.state.semaphores.empty.value).toBe(3); // back to 3
    expect(consumed.state.semaphores.full.value).toBe(0);
  });

  test('alternating produce/consume cycle', () => {
    let sim = createSimulation(3);

    // produce 2 items
    sim = producerStep(sim, '🍎').state;
    sim = producerStep(sim, '🍊').state;
    expect(sim.semaphores.full.value).toBe(2);
    expect(sim.semaphores.empty.value).toBe(1);

    // consume 1
    sim = consumerStep(sim).state;
    expect(sim.semaphores.full.value).toBe(1);
    expect(sim.semaphores.empty.value).toBe(2);
    expect(sim.buffer[0]).toBe(null);
    expect(sim.buffer[1]).toBe('🍊');
  });

  test('consumer wraps around circular buffer', () => {
    let sim = createSimulation(3);
    // fill buffer
    sim = producerStep(sim, 'a').state;
    sim = producerStep(sim, 'b').state;
    sim = producerStep(sim, 'c').state;
    // producer idx = 0 (wrapped)

    // consume all 3
    sim = consumerStep(sim).state;
    sim = consumerStep(sim).state;
    sim = consumerStep(sim).state;
    expect(sim.consumerIndex).toBe(0); // wrapped
    expect(sim.semaphores.full.value).toBe(0);
    expect(sim.semaphores.empty.value).toBe(3);
  });
});

describe('full cycle sequence', () => {
  test('producer then consumer full cycle with buffer=4', () => {
    let sim = createSimulation(4);

    // produce 4 items (fill buffer)
    const items = ['🍎', '🍊', '🍇', '🍋'];
    for (let i = 0; i < 4; i++) {
      const r = producerStep(sim, items[i]);
      expect(r.blocked).toBe(false);
      sim = r.state;
    }
    expect(sim.semaphores.empty.value).toBe(0);
    expect(sim.semaphores.full.value).toBe(4);

    // try producing — blocks
    const blockedP = producerStep(sim, '🍉');
    expect(blockedP.blocked).toBe(true);

    // consume 1 — frees a slot
    const c = consumerStep(sim);
    expect(c.blocked).toBe(false);
    sim = c.state;
    expect(sim.semaphores.empty.value).toBe(1);
    expect(sim.semaphores.full.value).toBe(3);

    // producer can now proceed
    const p2 = producerStep(sim, '🍉');
    expect(p2.blocked).toBe(false);
    sim = p2.state;
    expect(sim.semaphores.empty.value).toBe(0);
    expect(sim.semaphores.full.value).toBe(4);

    // consume all 4
    for (let i = 0; i < 4; i++) {
      const cr = consumerStep(sim);
      if (cr.blocked) break;
      sim = cr.state;
    }
    expect(sim.semaphores.empty.value).toBe(4);
    expect(sim.semaphores.full.value).toBe(0);
    expect(sim.buffer.every(s => s === null)).toBe(true);
  });

  test('consumer blocks when empty, producer unblocks it', () => {
    let sim = createSimulation(3);

    // try consuming — blocks
    const c1 = consumerStep(sim);
    expect(c1.blocked).toBe(true);

    // producer puts item
    sim = producerStep(sim, '🍎').state;
    expect(sim.semaphores.full.value).toBe(1);

    // consumer can now consume
    const c2 = consumerStep(sim);
    expect(c2.blocked).toBe(false);
    expect(c2.state.buffer[0]).toBe(null);
  });
});

describe('addRandomItems', () => {
  test('adds default count (bufferSize) items', () => {
    const sim = createSimulation(4);
    const result = addRandomItems(sim);
    expect(result.state.semaphores.empty.value).toBe(0);
    expect(result.state.semaphores.full.value).toBe(4);
    expect(result.state.steps.length).toBeGreaterThan(0);
  });

  test('adds specified number of items', () => {
    const sim = createSimulation(6);
    const result = addRandomItems(sim, 3);
    expect(result.state.semaphores.full.value).toBe(3);
    expect(result.state.semaphores.empty.value).toBe(3);
  });

  test('stops when buffer is full', () => {
    const sim = createSimulation(3);
    const result = addRandomItems(sim, 10);
    // only 3 fit
    expect(result.state.semaphores.empty.value).toBe(0);
    expect(result.state.semaphores.full.value).toBe(3);
  });
});

describe('resetSimulation', () => {
  test('resets to initial state', () => {
    let sim = createSimulation(4);
    sim = producerStep(sim, '🍎').state;
    sim = producerStep(sim, '🍊').state;
    sim = consumerStep(sim).state;
    expect(sim.stepCount).toBeGreaterThan(0);

    const reset = resetSimulation(sim);
    expect(reset.buffer.every(s => s === null)).toBe(true);
    expect(reset.producerIndex).toBe(0);
    expect(reset.consumerIndex).toBe(0);
    expect(reset.semaphores.empty.value).toBe(4);
    expect(reset.semaphores.full.value).toBe(0);
    expect(reset.semaphores.mutex.value).toBe(1);
    expect(reset.steps).toEqual([]);
    expect(reset.stepCount).toBe(0);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd /Users/charliepan/Downloads/my-miniapp && npx jest tests/utils/producer-consumer.test.js`
Expected: FAIL with "Cannot find module"

- [ ] **Step 3: Implement `utils/producer-consumer.js`**

```js
/**
 * 生产者-消费者模拟引擎（纯函数，不可变操作）
 *
 * 基于信号量（utils/semaphore.js）实现经典生产者-消费者问题：
 * - 有界缓冲区（环形队列）
 * - 三个信号量：mutex（互斥）/ empty（空闲槽计数）/ full（已满槽计数）
 * - P/V 序列：P(empty) → P(mutex) → 生产/消费 → V(mutex) → V(full)
 *
 * 所有操作返回新状态，不修改入参。
 */

const { createSemaphore, semWait, semSignal } = require('./semaphore');

const MIN_BUFFER = 2;
const MAX_BUFFER = 8;
const ITEMS = ['🍎', '🍊', '🍇', '🍋', '🍉', '🍓', '🍑', '🍒'];

let _nextLogId = 0;

/**
 * 创建初始模拟状态
 * @param {number} bufferSize 缓冲区大小（自动 clamp 到 [2, 8]）
 * @returns {Object} SimulationState
 */
function createSimulation(bufferSize) {
  const size = Math.max(MIN_BUFFER, Math.min(MAX_BUFFER, Math.floor(bufferSize) || MIN_BUFFER));
  _nextLogId = 0;
  return {
    buffer: Array(size).fill(null),
    bufferSize: size,
    producerIndex: 0,
    consumerIndex: 0,
    semaphores: {
      mutex: createSemaphore(1),
      empty: createSemaphore(size),
      full: createSemaphore(0)
    },
    steps: [],
    stepCount: 0
  };
}

/**
 * 生成日志条目
 */
function _makeLog(actor, action, opts) {
  const log = {
    id: _nextLogId++,
    timestamp: opts.stepCount,
    actor,
    action,
    blocked: opts.blocked || false,
    woken: opts.woken || false
  };
  if (opts.semaphoreName !== undefined) {
    log.semaphoreName = opts.semaphoreName;
    log.semaphoreBefore = opts.semaphoreBefore;
    log.semaphoreAfter = opts.semaphoreAfter;
  }
  if (opts.item !== undefined) {
    log.item = opts.item;
  }
  return log;
}

/**
 * 执行一次 PV 操作并记录日志
 */
function _doPV(semaphores, semName, type, owner, stepCount) {
  const sem = semaphores[semName];
  let result;
  if (type === 'P') {
    result = semWait(sem, owner);
  } else {
    result = semSignal(sem);
  }
  const newSems = Object.assign({}, semaphores, { [semName]: result.semaphore });
  const log = _makeLog(owner, type + '(' + semName + ')', {
    stepCount,
    semaphoreName: semName,
    semaphoreBefore: sem.value,
    semaphoreAfter: result.semaphore.value,
    blocked: result.blocked || false,
    woken: result.woken !== null && result.woken !== undefined
  });
  return { semaphores: newSems, log, blocked: result.blocked, woken: result.woken };
}

/**
 * 生产者执行一次生产
 * @param {Object} state 当前模拟状态
 * @param {string} item 生产的物品（emoji 字符串）
 * @returns {{ state: Object, logs: Array, blocked: boolean }}
 */
function producerStep(state, item) {
  if (state.semaphores.empty.value <= 0) {
    // 缓冲区满，生产者阻塞
    const blockedLog = _makeLog('producer', '阻塞等待 empty', {
      stepCount: state.stepCount,
      blocked: true
    });
    return {
      state: Object.assign({}, state, {
        steps: state.steps.concat([blockedLog])
      }),
      logs: [blockedLog],
      blocked: true
    };
  }

  const stepCount = state.stepCount;
  const logs = [];
  let curSems = state.semaphores;

  // 1. P(empty)
  const pEmpty = _doPV(curSems, 'empty', 'P', 'producer', stepCount);
  curSems = pEmpty.semaphores;
  logs.push(pEmpty.log);

  // 2. P(mutex)
  const pMutex = _doPV(curSems, 'mutex', 'P', 'producer', stepCount);
  curSems = pMutex.semaphores;
  logs.push(pMutex.log);

  // 3. 放入缓冲区
  const newBuffer = state.buffer.slice();
  const prodIdx = state.producerIndex;
  newBuffer[prodIdx] = item;
  const newProdIdx = (prodIdx + 1) % state.bufferSize;
  const produceLog = _makeLog('producer', '生产 ' + item, {
    stepCount,
    item
  });
  logs.push(produceLog);

  // 4. V(mutex)
  const vMutex = _doPV(curSems, 'mutex', 'V', 'producer', stepCount);
  curSems = vMutex.semaphores;
  logs.push(vMutex.log);

  // 5. V(full)
  const vFull = _doPV(curSems, 'full', 'V', 'producer', stepCount);
  curSems = vFull.semaphores;
  logs.push(vFull.log);

  return {
    state: {
      buffer: newBuffer,
      bufferSize: state.bufferSize,
      producerIndex: newProdIdx,
      consumerIndex: state.consumerIndex,
      semaphores: curSems,
      steps: state.steps.concat(logs),
      stepCount: stepCount + 1
    },
    logs,
    blocked: false
  };
}

/**
 * 消费者执行一次消费
 * @param {Object} state 当前模拟状态
 * @returns {{ state: Object, logs: Array, blocked: boolean }}
 */
function consumerStep(state) {
  if (state.semaphores.full.value <= 0) {
    // 缓冲区空，消费者阻塞
    const blockedLog = _makeLog('consumer', '阻塞等待 full', {
      stepCount: state.stepCount,
      blocked: true
    });
    return {
      state: Object.assign({}, state, {
        steps: state.steps.concat([blockedLog])
      }),
      logs: [blockedLog],
      blocked: true
    };
  }

  const stepCount = state.stepCount;
  const logs = [];
  let curSems = state.semaphores;

  // 1. P(full)
  const pFull = _doPV(curSems, 'full', 'P', 'consumer', stepCount);
  curSems = pFull.semaphores;
  logs.push(pFull.log);

  // 2. P(mutex)
  const pMutex = _doPV(curSems, 'mutex', 'P', 'consumer', stepCount);
  curSems = pMutex.semaphores;
  logs.push(pMutex.log);

  // 3. 取出物品
  const newBuffer = state.buffer.slice();
  const consIdx = state.consumerIndex;
  const item = newBuffer[consIdx];
  newBuffer[consIdx] = null;
  const newConsIdx = (consIdx + 1) % state.bufferSize;
  const consumeLog = _makeLog('consumer', '消费 ' + (item || '?'), {
    stepCount,
    item: item || ''
  });
  logs.push(consumeLog);

  // 4. V(mutex)
  const vMutex = _doPV(curSems, 'mutex', 'V', 'consumer', stepCount);
  curSems = vMutex.semaphores;
  logs.push(vMutex.log);

  // 5. V(empty)
  const vEmpty = _doPV(curSems, 'empty', 'V', 'consumer', stepCount);
  curSems = vEmpty.semaphores;
  logs.push(vEmpty.log);

  return {
    state: {
      buffer: newBuffer,
      bufferSize: state.bufferSize,
      producerIndex: state.producerIndex,
      consumerIndex: newConsIdx,
      semaphores: curSems,
      steps: state.steps.concat(logs),
      stepCount: stepCount + 1
    },
    logs,
    blocked: false
  };
}

/**
 * 批量执行多次生产（随机物品）
 * @param {Object} state
 * @param {number} [count] 生产次数，默认 fill buffer
 * @returns {{ state: Object, logs: Array }}
 */
function addRandomItems(state, count) {
  let s = state;
  const allLogs = [];
  const total = count !== undefined ? count : state.bufferSize;
  let produced = 0;
  for (let i = 0; i < total; i++) {
    if (s.semaphores.empty.value <= 0) break;
    const item = ITEMS[Math.floor(Math.random() * ITEMS.length)];
    const r = producerStep(s, item);
    s = r.state;
    allLogs.push.apply(allLogs, r.logs);
    produced++;
  }
  return { state: s, logs: allLogs };
}

/**
 * 重置模拟状态到初始
 * @param {Object} state
 * @returns {Object} 新的初始状态
 */
function resetSimulation(state) {
  _nextLogId = 0;
  return createSimulation(state.bufferSize);
}

module.exports = {
  createSimulation,
  producerStep,
  consumerStep,
  addRandomItems,
  resetSimulation
};
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd /Users/charliepan/Downloads/my-miniapp && npx jest tests/utils/producer-consumer.test.js`
Expected: PASS (all tests)

- [ ] **Step 5: Run both semaphore + producer-consumer tests together**

Run: `cd /Users/charliepan/Downloads/my-miniapp && npx jest tests/utils/semaphore.test.js tests/utils/producer-consumer.test.js`
Expected: ALL PASS

- [ ] **Step 6: Commit**

```bash
git add utils/producer-consumer.js tests/utils/producer-consumer.test.js
git commit -m "feat(producer-consumer): 生产者-消费者模拟引擎（PV 序列 + 环形缓冲区 + 阻塞/唤醒）"
```

---

## Task 3: 页面 4 文件（json / wxml / wxss / js）

**Files:**
- Create: `pages/sync-viz/sync-viz.json`
- Create: `pages/sync-viz/sync-viz.wxml`
- Create: `pages/sync-viz/sync-viz.wxss`
- Create: `pages/sync-viz/sync-viz.js`

**Interfaces:**
- Consumes:
  - `createSimulation`, `producerStep`, `consumerStep`, `addRandomItems`, `resetSimulation` from `utils/producer-consumer.js`
- Produces: 完整的 `pages/sync-viz/` 页面（4 文件）

**Page `data` 结构：**

```js
{
  bufferSize: 6,               // 缓冲区大小（滑块值）
  producerInterval: 3,         // 生产间隔（秒）
  consumerInterval: 5,         // 消费间隔（秒）
  simulation: null,            // SimulationState
  buffer: [],                  // 当前缓冲区快照（用于 WXML 渲染）
  semaphores: {                // 信号量当前值（用于 WXML 渲染）
    full: 0, empty: 6, mutex: 1
  },
  semaphoreBars: {             // 信号量可视化条数据
    full: { value: 0, max: 6, color: '#81b29a' },
    empty: { value: 6, max: 6, color: '#8d99ae' },
    mutex: { value: 1, max: 1, color: '#cc785c' }
  },
  steps: [],                   // 操作日志
  waiting: {                   // 阻塞状态
    producer: false,
    consumer: false
  },
  running: false,              // 是否自动运行中
  paused: false,               // 是否暂停
  speedIndex: 1,               // 0=0.5x, 1=1x, 2=2x
  stepMode: false,             // 步进模式
  lastAction: ''               // 最近操作描述
}
```

- [ ] **Step 1: Write `pages/sync-viz/sync-viz.json`**

```json
{
  "navigationBarTitleText": "同步互斥演示",
  "navigationBarBackgroundColor": "#faf9f5",
  "navigationBarTextStyle": "black"
}
```

- [ ] **Step 2: Write `pages/sync-viz/sync-viz.wxml`**

```xml
<view class="page">

  <!-- 参数配置 -->
  <view class="config-band">
    <text class="band-label">同步互斥演示</text>
    <text class="band-subtitle">生产者-消费者 · 信号量 PV 操作</text>
    <view class="config-sliders">
      <view class="config-item">
        <text class="config-label">缓冲区大小</text>
        <view class="slider-row">
          <slider class="config-slider" min="2" max="8" step="1" value="{{bufferSize}}"
                  activeColor="#cc785c" backgroundColor="#e6dfd8" bindchange="onBufferSizeChange" />
          <text class="slider-value">{{bufferSize}}</text>
        </view>
      </view>
      <view class="config-item">
        <text class="config-label">生产间隔</text>
        <view class="slider-row">
          <slider class="config-slider" min="1" max="8" step="1" value="{{producerInterval}}"
                  activeColor="#cc785c" backgroundColor="#e6dfd8" bindchange="onProducerIntervalChange" />
          <text class="slider-value">{{producerInterval}}s</text>
        </view>
      </view>
      <view class="config-item">
        <text class="config-label">消费间隔</text>
        <view class="slider-row">
          <slider class="config-slider" min="1" max="8" step="1" value="{{consumerInterval}}"
                  activeColor="#cc785c" backgroundColor="#e6dfd8" bindchange="onConsumerIntervalChange" />
          <text class="slider-value">{{consumerInterval}}s</text>
        </view>
      </view>
    </view>
  </view>

  <!-- 主体区：缓冲区 + 信号量 -->
  <view class="main-band">
    <!-- 左侧：缓冲区 -->
    <view class="buffer-section">
      <text class="section-title">缓冲区</text>
      <view class="buffer-grid">
        <view wx:for="{{buffer}}" wx:key="index" wx:for-index="idx"
              class="buffer-slot {{item ? 'buffer-slot-filled' : 'buffer-slot-empty'}}"
              style="transition-delay: {{idx * 50}}ms">
          <text wx:if="{{item}}" class="buffer-item">{{item}}</text>
          <text wx:else class="buffer-empty-indicator">·</text>
          <text class="slot-index">{{idx}}</text>
        </view>
      </view>
      <!-- 生产者 / 消费者指针 -->
      <view class="pointer-row">
        <view class="pointer-item pointer-producer">
          <text class="pointer-label">生产者 →</text>
          <text class="pointer-arrow" style="margin-left: {{producerIndex * (100 / bufferSize)}}%">▼</text>
        </view>
        <view class="pointer-item pointer-consumer">
          <text class="pointer-label">消费者 →</text>
          <text class="pointer-arrow consumer-arrow" style="margin-left: {{consumerIndex * (100 / bufferSize)}}%">▲</text>
        </view>
      </view>
      <!-- 阻塞状态 -->
      <view wx:if="{{waiting.producer || waiting.consumer}}" class="waiting-banner">
        <text wx:if="{{waiting.producer}}" class="waiting-text producer-waiting">⏳ 生产者阻塞等待 empty</text>
        <text wx:if="{{waiting.consumer}}" class="waiting-text consumer-waiting">⏳ 消费者阻塞等待 full</text>
      </view>
    </view>

    <!-- 右侧：信号量 -->
    <view class="semaphore-section">
      <text class="section-title">信号量</text>
      <view class="semaphore-list">
        <view class="semaphore-item">
          <view class="semaphore-header">
            <text class="semaphore-name" style="color: {{semaphoreBars.full.color}}">full</text>
            <text class="semaphore-value">{{semaphores.full}} / {{bufferSize}}</text>
          </view>
          <view class="semaphore-track">
            <view class="semaphore-bar" style="width: {{semaphoreBars.full.percent}}%; background: {{semaphoreBars.full.color}}; transition: width 0.3s ease"></view>
          </view>
        </view>
        <view class="semaphore-item">
          <view class="semaphore-header">
            <text class="semaphore-name" style="color: {{semaphoreBars.empty.color}}">empty</text>
            <text class="semaphore-value">{{semaphores.empty}} / {{bufferSize}}</text>
          </view>
          <view class="semaphore-track">
            <view class="semaphore-bar" style="width: {{semaphoreBars.empty.percent}}%; background: {{semaphoreBars.empty.color}}; transition: width 0.3s ease"></view>
          </view>
        </view>
        <view class="semaphore-item">
          <view class="semaphore-header">
            <text class="semaphore-name" style="color: {{semaphoreBars.mutex.color}}">mutex</text>
            <text class="semaphore-value">{{semaphores.mutex}} / 1</text>
          </view>
          <view class="semaphore-track">
            <view class="semaphore-bar" style="width: {{semaphoreBars.mutex.percent}}%; background: {{semaphoreBars.mutex.color}}; transition: width 0.3s ease"></view>
          </view>
        </view>
      </view>
      <!-- 等待队列 -->
      <view wx:if="{{waiting.producer || waiting.consumer}}" class="wait-queue">
        <text class="wait-queue-title">等待队列：</text>
        <text wx:if="{{waiting.producer}}" class="wait-queue-item">生产者</text>
        <text wx:if="{{waiting.consumer}}" class="wait-queue-item">消费者</text>
      </view>
    </view>
  </view>

  <!-- 操作日志 -->
  <view class="log-band">
    <view class="log-header">
      <text class="section-title">操作日志</text>
      <text class="log-count">{{steps.length}} 条</text>
    </view>
    <scroll-view class="log-scroll" scroll-y scroll-into-view="{{'log-' + (steps.length > 0 ? steps.length - 1 : 0)}}">
      <view wx:if="{{steps.length === 0}}" class="log-empty">
        <text class="log-empty-text">点击 ▶ 或步进开始模拟</text>
      </view>
      <view wx:for="{{steps}}" wx:key="id" wx:for-index="idx" class="log-row {{item.actor === 'producer' ? 'log-producer' : 'log-consumer'}}" id="{{'log-' + idx}}">
        <text class="log-time">[t={{item.timestamp}}]</text>
        <text class="log-actor">{{item.actor === 'producer' ? '生产者' : '消费者'}}</text>
        <text class="log-action {{item.action.indexOf('阻塞') !== -1 ? 'log-action-blocked' : ''}}">{{item.action}}</text>
        <text wx:if="{{item.semaphoreName !== undefined}}" class="log-semaphore">{{item.semaphoreName}}: {{item.semaphoreBefore}}→{{item.semaphoreAfter}}</text>
        <text wx:if="{{item.blocked}}" class="log-blocked-badge">阻塞</text>
        <text wx:if="{{item.woken}}" class="log-woken-badge">唤醒</text>
      </view>
    </scroll-view>
  </view>

  <!-- 控制栏 -->
  <view class="ctrl-band">
    <view class="ctrl-btn ctrl-reset" bindtap="onReset">
      <text class="ctrl-icon">↻</text>
    </view>
    <view class="ctrl-btn ctrl-play {{running ? 'ctrl-playing' : ''}}" bindtap="onTogglePlay">
      <text class="ctrl-icon">{{running ? (paused ? '▶' : '⏸') : '▶'}}</text>
    </view>
    <view class="ctrl-btn" bindtap="onStep">
      <text class="ctrl-icon">⏭</text>
    </view>
    <view class="speed-area">
      <text class="speed-label">速度</text>
      <view class="speed-buttons">
        <view class="speed-btn {{speedIndex === 0 ? 'speed-active' : ''}}" bindtap="onSpeedChange" data-index="0">0.5x</view>
        <view class="speed-btn {{speedIndex === 1 ? 'speed-active' : ''}}" bindtap="onSpeedChange" data-index="1">1x</view>
        <view class="speed-btn {{speedIndex === 2 ? 'speed-active' : ''}}" bindtap="onSpeedChange" data-index="2">2x</view>
      </view>
    </view>
  </view>

</view>
```

- [ ] **Step 3: Write `pages/sync-viz/sync-viz.wxss`**

```css
/* ======================== 页面基础 ======================== */
.page {
  min-height: 100vh;
  background: #faf9f5;
  padding: 20rpx 24rpx 40rpx;
  font-family: -apple-system, BlinkMacSystemFont, 'Georgia', serif;
  color: #141413;
}

.band-label {
  display: block;
  font-family: Georgia, 'Times New Roman', serif;
  font-size: 32rpx;
  font-weight: 400;
  letter-spacing: -3rpx;
  color: #141413;
  margin-bottom: 6rpx;
}

.band-subtitle {
  display: block;
  font-size: 22rpx;
  color: #6c6a64;
  margin-bottom: 20rpx;
  font-family: -apple-system, BlinkMacSystemFont, sans-serif;
  letter-spacing: 0;
}

/* ======================== 参数配置 ======================== */
.config-band {
  background: #efe9de;
  border-radius: 24rpx;
  padding: 28rpx 24rpx;
  margin-bottom: 20rpx;
}

.config-sliders {
  display: flex;
  flex-direction: column;
  gap: 12rpx;
}

.config-item {
  background: #faf9f5;
  border-radius: 16rpx;
  padding: 16rpx 20rpx;
}

.config-label {
  display: block;
  font-size: 24rpx;
  color: #6c6a64;
  margin-bottom: 4rpx;
}

.slider-row {
  display: flex;
  align-items: center;
  gap: 16rpx;
}

.config-slider {
  flex: 1;
  margin: 0;
}

.slider-value {
  font-size: 26rpx;
  font-weight: 600;
  color: #141413;
  font-family: 'SF Mono', 'Menlo', monospace;
  width: 56rpx;
  text-align: right;
  flex-shrink: 0;
}

/* ======================== 主体区 ======================== */
.main-band {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
  margin-bottom: 20rpx;
}

@media (min-width: 600px) {
  .main-band {
    flex-direction: row;
  }
}

.section-title {
  display: block;
  font-size: 26rpx;
  font-weight: 700;
  color: #141413;
  font-family: Georgia, serif;
  margin-bottom: 16rpx;
}

/* ======================== 缓冲区 ======================== */
.buffer-section {
  background: #efe9de;
  border-radius: 24rpx;
  padding: 24rpx;
  flex: 1;
}

.buffer-grid {
  display: flex;
  gap: 8rpx;
  margin-bottom: 20rpx;
}

.buffer-slot {
  flex: 1;
  aspect-ratio: 1;
  border-radius: 16rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  position: relative;
  transition: all 0.3s ease;
  min-height: 100rpx;
}

.buffer-slot-empty {
  background: #faf9f5;
  border: 2rpx dashed #d4cfc2;
}

.buffer-slot-filled {
  background: #e8dccb;
  border: 2rpx solid #cc785c;
  box-shadow: 0 0 0 2rpx rgba(204, 120, 92, 0.15);
}

.buffer-item {
  font-size: 40rpx;
  line-height: 1;
}

.buffer-empty-indicator {
  font-size: 24rpx;
  color: #d4cfc2;
}

.slot-index {
  position: absolute;
  bottom: 4rpx;
  right: 8rpx;
  font-size: 16rpx;
  color: #8e8b82;
  font-family: 'SF Mono', 'Menlo', monospace;
}

/* ======================== 指针行 ======================== */
.pointer-row {
  margin-bottom: 12rpx;
}

.pointer-item {
  display: flex;
  align-items: center;
  gap: 8rpx;
  margin-bottom: 4rpx;
  height: 36rpx;
}

.pointer-label {
  font-size: 20rpx;
  color: #6c6a64;
  flex-shrink: 0;
  width: 96rpx;
}

.pointer-arrow {
  font-size: 24rpx;
  color: #cc785c;
  transition: margin-left 0.3s ease;
}

.consumer-arrow {
  color: #8d99ae;
}

/* ======================== 阻塞指示 ======================== */
.waiting-banner {
  margin-top: 8rpx;
  display: flex;
  flex-direction: column;
  gap: 4rpx;
}

.waiting-text {
  font-size: 22rpx;
  padding: 8rpx 16rpx;
  border-radius: 12rpx;
  text-align: center;
}

.producer-waiting {
  background: #fde8d6;
  color: #a9583e;
}

.consumer-waiting {
  background: #d6e4f0;
  color: #4a6fa5;
}

/* ======================== 信号量 ======================== */
.semaphore-section {
  background: #efe9de;
  border-radius: 24rpx;
  padding: 24rpx;
  flex: 1;
}

.semaphore-list {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
}

.semaphore-item {
  background: #faf9f5;
  border-radius: 16rpx;
  padding: 16rpx;
}

.semaphore-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 8rpx;
}

.semaphore-name {
  font-size: 24rpx;
  font-weight: 600;
  font-family: 'SF Mono', 'Menlo', monospace;
}

.semaphore-value {
  font-size: 22rpx;
  color: #6c6a64;
  font-family: 'SF Mono', 'Menlo', monospace;
}

.semaphore-track {
  height: 24rpx;
  background: #e6dfd8;
  border-radius: 12rpx;
  overflow: hidden;
}

.semaphore-bar {
  height: 100%;
  border-radius: 12rpx;
  min-width: 0;
  transition: width 0.3s ease;
}

/* ======================== 等待队列 ======================== */
.wait-queue {
  margin-top: 16rpx;
  display: flex;
  align-items: center;
  gap: 8rpx;
  flex-wrap: wrap;
}

.wait-queue-title {
  font-size: 22rpx;
  color: #6c6a64;
}

.wait-queue-item {
  font-size: 20rpx;
  padding: 4rpx 12rpx;
  border-radius: 8rpx;
  background: #faf9f5;
  color: #a9583e;
  border: 2rpx solid #cc785c;
}

/* ======================== 操作日志 ======================== */
.log-band {
  background: #efe9de;
  border-radius: 24rpx;
  padding: 24rpx;
  margin-bottom: 20rpx;
}

.log-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 12rpx;
}

.log-count {
  font-size: 20rpx;
  color: #8e8b82;
  font-family: 'SF Mono', 'Menlo', monospace;
}

.log-scroll {
  max-height: 400rpx;
  overflow-y: auto;
}

.log-empty {
  display: flex;
  align-items: center;
  justify-content: center;
  height: 120rpx;
}

.log-empty-text {
  font-size: 24rpx;
  color: #8e8b82;
}

.log-row {
  display: flex;
  align-items: center;
  gap: 8rpx;
  padding: 8rpx 12rpx;
  border-radius: 8rpx;
  margin-bottom: 4rpx;
  font-size: 22rpx;
  font-family: 'SF Mono', 'Menlo', monospace;
  flex-wrap: wrap;
}

.log-producer {
  background: rgba(252, 232, 218, 0.5);
  border-left: 4rpx solid #cc785c;
}

.log-consumer {
  background: rgba(214, 228, 240, 0.4);
  border-left: 4rpx solid #8d99ae;
}

.log-time {
  color: #8e8b82;
  flex-shrink: 0;
}

.log-actor {
  font-weight: 600;
  color: #141413;
  flex-shrink: 0;
}

.log-action {
  color: #141413;
  flex-shrink: 0;
}

.log-action-blocked {
  color: #c0392b;
  font-weight: 600;
}

.log-semaphore {
  color: #6c6a64;
  font-size: 20rpx;
}

.log-blocked-badge {
  font-size: 18rpx;
  padding: 2rpx 8rpx;
  border-radius: 6rpx;
  background: #f5d9d3;
  color: #c0392b;
}

.log-woken-badge {
  font-size: 18rpx;
  padding: 2rpx 8rpx;
  border-radius: 6rpx;
  background: #d4edda;
  color: #27ae60;
}

/* ======================== 控制栏 ======================== */
.ctrl-band {
  display: flex;
  align-items: center;
  gap: 16rpx;
  background: #efe9de;
  border-radius: 24rpx;
  padding: 20rpx 24rpx;
  margin-bottom: 40rpx;
}

.ctrl-btn {
  width: 72rpx;
  height: 72rpx;
  border-radius: 50%;
  background: #faf9f5;
  display: flex;
  align-items: center;
  justify-content: center;
}

.ctrl-btn:active { background: #e8e0d2; }

.ctrl-play {
  background: #cc785c;
  width: 88rpx;
  height: 88rpx;
}

.ctrl-playing {
  background: #a9583e;
}

.ctrl-play:active { background: #a9583e; }

.ctrl-play .ctrl-icon { color: #fff; font-size: 32rpx; }

.ctrl-icon {
  font-size: 28rpx;
  color: #141413;
}

.ctrl-reset {
  background: #faf9f5;
}

.speed-area {
  flex: 1;
  display: flex;
  align-items: center;
  gap: 8rpx;
  margin-left: 8rpx;
}

.speed-label {
  font-size: 22rpx;
  color: #6c6a64;
  flex-shrink: 0;
}

.speed-buttons {
  display: flex;
  gap: 6rpx;
  flex: 1;
}

.speed-btn {
  flex: 1;
  padding: 12rpx 16rpx;
  background: #faf9f5;
  border-radius: 12rpx;
  text-align: center;
  font-size: 22rpx;
  color: #6c6a64;
  font-family: 'SF Mono', 'Menlo', monospace;
}

.speed-active {
  background: #cc785c;
  color: #fff;
}
```

- [ ] **Step 4: Write `pages/sync-viz/sync-viz.js`**

```js
const {
  createSimulation,
  producerStep,
  consumerStep,
  addRandomItems,
  resetSimulation
} = require('../../utils/producer-consumer');

const ITEMS = ['🍎', '🍊', '🍇', '🍋', '🍉', '🍓', '🍑', '🍒'];
const SPEED_OPTIONS = [
  { label: '0.5x', delayMs: 2000 },
  { label: '1x',   delayMs: 1000 },
  { label: '2x',   delayMs: 500 }
];

Page({
  data: {
    bufferSize: 6,
    producerInterval: 3,
    consumerInterval: 5,
    simulation: null,
    buffer: [],
    semaphores: { full: 0, empty: 6, mutex: 1 },
    semaphoreBars: {
      full: { value: 0, max: 6, percent: 0, color: '#81b29a' },
      empty: { value: 6, max: 6, percent: 100, color: '#8d99ae' },
      mutex: { value: 1, max: 1, percent: 100, color: '#cc785c' }
    },
    steps: [],
    waiting: { producer: false, consumer: false },
    producerIndex: 0,
    consumerIndex: 0,
    running: false,
    paused: false,
    speedIndex: 1
  },

  _sim: null,
  _timer: null,
  _producerTimer: null,
  _consumerTimer: null,
  _stepCounter: 0,

  onLoad() {
    this._initSimulation();
  },

  onUnload() {
    this._stopAll();
  },

  _initSimulation() {
    this._sim = createSimulation(this.data.bufferSize);
    this._stepCounter = 0;
    this._syncState();
  },

  _syncState() {
    if (!this._sim) return;
    const buffer = this._sim.buffer.slice();
    const bufferSize = this._sim.bufferSize;
    const sems = this._sim.semaphores;

    const fullPercent = bufferSize > 0 ? (sems.full.value / bufferSize) * 100 : 0;
    const emptyPercent = bufferSize > 0 ? (sems.empty.value / bufferSize) * 100 : 0;
    const mutexPercent = sems.mutex.value >= 0 ? sems.mutex.value * 100 : 0;

    this.setData({
      simulation: this._sim,
      buffer,
      semaphores: {
        full: sems.full.value,
        empty: sems.empty.value,
        mutex: sems.mutex.value
      },
      semaphoreBars: {
        full: { value: sems.full.value, max: bufferSize, percent: Math.max(0, fullPercent), color: '#81b29a' },
        empty: { value: sems.empty.value, max: bufferSize, percent: Math.max(0, emptyPercent), color: '#8d99ae' },
        mutex: { value: sems.mutex.value, max: 1, percent: Math.max(0, mutexPercent), color: '#cc785c' }
      },
      steps: this._sim.steps,
      producerIndex: this._sim.producerIndex,
      consumerIndex: this._sim.consumerIndex,
      waiting: {
        producer: this._sim.semaphores.empty.value <= 0,
        consumer: this._sim.semaphores.full.value <= 0
      }
    });
  },

  onBufferSizeChange(e) {
    const val = Number(e.detail.value);
    if (val === this.data.bufferSize) return;
    this._stopAll();
    this.setData({ bufferSize: val });
    this._initSimulation();
  },

  onProducerIntervalChange(e) {
    this.setData({ producerInterval: Number(e.detail.value) });
  },

  onConsumerIntervalChange(e) {
    this.setData({ consumerInterval: Number(e.detail.value) });
  },

  onTogglePlay() {
    if (this.data.running && !this.data.paused) {
      // pause
      this._pauseAll();
    } else if (this.data.running && this.data.paused) {
      // resume
      this._resumeAll();
    } else {
      // start
      this._startAuto();
    }
  },

  onStep() {
    this._stopAll();
    // reset if simulation has run to completion state
    if (this._stepCounter > 50) {
      this._initSimulation();
    }
    this._doOneStep();
    this.setData({
      running: false,
      paused: false
    });
  },

  onReset() {
    this._stopAll();
    this._initSimulation();
    this._stepCounter = 0;
    this.setData({
      running: false,
      paused: false
    });
  },

  onSpeedChange(e) {
    this.setData({ speedIndex: Number(e.currentTarget.dataset.index) });
    // if running, restart with new speed
    if (this.data.running && !this.data.paused) {
      this._stopTimers();
      this._startTimers();
    }
  },

  _doOneStep() {
    if (!this._sim) return;
    // alternate: producer then consumer
    const isProducer = this._stepCounter % 2 === 0;
    let result;
    if (isProducer) {
      const item = ITEMS[Math.floor(Math.random() * ITEMS.length)];
      result = producerStep(this._sim, item);
    } else {
      result = consumerStep(this._sim);
    }
    if (!result.blocked) {
      this._sim = result.state;
    }
    this._stepCounter++;
    this._syncState();
  },

  _startAuto() {
    if (this._stepCounter > 50) {
      this._initSimulation();
    }
    this.setData({ running: true, paused: false });
    this._startTimers();
  },

  _startTimers() {
    const speedIdx = this.data.speedIndex;
    const producerDelay = this.data.producerInterval * SPEED_OPTIONS[speedIdx].delayMs / 1000;
    const consumerDelay = this.data.consumerInterval * SPEED_OPTIONS[speedIdx].delayMs / 1000;

    this._producerTimer = setInterval(() => {
      if (!this._sim) return;
      const item = ITEMS[Math.floor(Math.random() * ITEMS.length)];
      const result = producerStep(this._sim, item);
      if (!result.blocked) {
        this._sim = result.state;
        this._stepCounter++;
      }
      this._syncState();
      if (this._stepCounter > 100) {
        this._stopAll();
      }
    }, producerDelay);

    this._consumerTimer = setInterval(() => {
      if (!this._sim) return;
      const result = consumerStep(this._sim);
      if (!result.blocked) {
        this._sim = result.state;
        this._stepCounter++;
      }
      this._syncState();
    }, consumerDelay);
  },

  _pauseAll() {
    this._stopTimers();
    this.setData({ paused: true });
  },

  _resumeAll() {
    this.setData({ paused: false });
    this._startTimers();
  },

  _stopTimers() {
    if (this._producerTimer) {
      clearInterval(this._producerTimer);
      this._producerTimer = null;
    }
    if (this._consumerTimer) {
      clearInterval(this._consumerTimer);
      this._consumerTimer = null;
    }
  },

  _stopAll() {
    this._stopTimers();
    this.setData({ running: false, paused: false });
  }
});
```

- [ ] **Step 5: Verify utils tests still pass**

Run: `cd /Users/charliepan/Downloads/my-miniapp && npm test`
Expected: ALL PASS

- [ ] **Step 6: Commit**

```bash
git add pages/sync-viz/
git commit -m "feat(sync-viz): 页面 4 文件（json/wxml/wxss/js），含缓冲区可视化 + 信号量条 + PV 日志 + 播放控制"
```

---

## Task 4: 注册 + tool-registry + 模块文档

**Files:**
- Modify: `app.json`（添加 `pages/sync-viz/sync-viz`）
- Modify: `utils/tool-registry.js`（`sync-viz.available = true`）
- Create: `docs/handoff/modules/sync-viz.md`

**Interfaces:**
- Consumes: `sync-viz` 工具的现有元数据（已在 tool-registry.js 占位）
- Produces: 工具在首页可见，模块文档就位

- [ ] **Step 1: Modify `app.json`**

在 `pages` 数组中按字母序插入 `"pages/sync-viz/sync-viz"`。在 `subnet-calc` 之后，`tcp-viz` 之前。插入后 `pages` 数组应为：

```json
[
  "pages/index/index",
  "pages/quiz-list/quiz-list",
  "pages/import-preview/import-preview",
  "pages/quiz/quiz",
  "pages/result/result",
  "pages/records/records",
  "pages/record-detail/record-detail",
  "pages/wrong-questions/wrong-questions",
  "pages/dashboard/dashboard",
  "pages/cpu-sched/cpu-sched",
  "pages/subnet-calc/subnet-calc",
  "pages/sync-viz/sync-viz",
  "pages/sort-viz/sort-viz",
  "pages/tcp-viz/tcp-viz",
  "pages/ds-viz/ds-viz"
]
```

- [ ] **Step 2: Modify `utils/tool-registry.js`**

找到 `sync-viz` 工具对象，把 `available: false` 改为 `available: true`；其余字段（id / category / name / icon / description / route / order）保持不变。

- [ ] **Step 3: Write `docs/handoff/modules/sync-viz.md`**

```md
# 同步互斥演示（Sync Viz — Producer-Consumer + Semaphore PV）

> 由 `2026-07-19-sync-viz-design.md` spec 实施落盘。

## 概览

交互式演示经典生产者-消费者问题，通过信号量的 P/V 操作可视化进程同步过程。适合 OS 学习者理解临界区、信号量机制和经典同步问题。

## 数据驱动

| 模块 | 内容 |
|---|---|
| `utils/semaphore.js` | 信号量数据结构 + 不可变 P/V 操作（`semWait` / `semSignal`），含 FIFO 等待队列和操作历史 |
| `utils/producer-consumer.js` | 生产者-消费者模拟引擎：`createSimulation` / `producerStep` / `consumerStep` / `addRandomItems` / `resetSimulation` |

## 核心概念

| 概念 | 实现 |
|---|---|
| 信号量 | 三个信号量：`mutex`（互斥，初始 1）、`empty`（空闲槽，初始 = bufferSize）、`full`（已满槽，初始 0） |
| P 操作 | `semWait`：value--；若 value<0 则将当前线程加入等待队列（阻塞） |
| V 操作 | `semSignal`：value++；若 value<=0 则从等待队列 FIFO 唤醒一个线程 |
| 生产者流程 | P(empty) → P(mutex) → 放入物品 → V(mutex) → V(full) |
| 消费者流程 | P(full) → P(mutex) → 取出物品 → V(mutex) → V(empty) |
| 环形缓冲区 | 通过 `producerIndex` / `consumerIndex` 指针实现循环读写 |

## 信号量可视化

| 信号量 | 颜色 | 说明 |
|---|---|---|
| full  | `#81b29a`（鼠尾草绿） | 当前已占用的缓冲区 slot 数 |
| empty | `#8d99ae`（钢蓝） | 当前空闲的缓冲区 slot 数 |
| mutex | `#cc785c`（珊瑚） | 互斥锁状态（0=被占用，1=空闲） |

每个信号量以横向进度条显示当前值百分比，值变化时 WXSS `transition` 动画过渡。

## 缓冲区渲染

环形缓冲区使用 flex 横向排列 `<view>` 格子：
- 空 slot：虚线边框 + 半透明背景
- 已填充 slot：实线边框 + 浅色背景 + emoji 物品图标
- 生产者指针（▼）和消费者指针（▲）标注在当前操作位置

## 操作日志

底部 `<scroll-view>` 自动滚动到最新条目。每条日志显示：
- 时间戳 `[t=...]`
- 操作者（生产者 / 消费者）
- 操作描述（P(empty) / V(full) / 生产 🍎 等）
- 信号量值变化（before→after）
- 阻塞/唤醒标识（颜色高亮）

阻塞时日志条目标红色，唤醒时标绿色。

## 设计风格

完全 Claude Design 暖奶油画布（`#faf9f5` 背景 / `#efe9de` 卡片 / `#cc785c` CTA / Georgia 衬线标题）。信号量颜色沿用冷调（绿/蓝）以区别于主色珊瑚，避免语义冲突。

## 测试

- `tests/utils/semaphore.test.js` —— 信号量创建 / P/V 操作 / 阻塞 / FIFO 唤醒 / 不可变性（~25 测试）
- `tests/utils/producer-consumer.test.js` —— 生产消费流程 / 缓冲区满阻塞 / 缓冲区空阻塞 / 环形 wrap / 重置（~20 测试）

`npm test` 全绿。

## PV 操作序列（关键实现细节）

```
生产者                   消费者
P(empty)  ←↓             P(full)   ←↓
P(mutex)  ←↓             P(mutex)  ←↓
放入物品                   取出物品
V(mutex)  ↑→             V(mutex)  ↑→
V(full)   ↑→             V(empty)  ↑→
```

阻塞条件：
- 生产者：`empty.value ≤ 0`（缓冲区满）
- 消费者：`full.value ≤ 0`（缓冲区空）
```

- [ ] **Step 4: Run full test suite**

Run: `cd /Users/charliepan/Downloads/my-miniapp && npm test`
Expected: ALL PASS

- [ ] **Step 5: Commit**

```bash
git add app.json utils/tool-registry.js docs/handoff/modules/sync-viz.md
git commit -m "feat(sync-viz): 注册页面 + tool-registry 可用化 + 模块文档"
```

---

## Task 5: 验证完成 + HANDOFF 同步

**Files:**
- Modify: `.claude/HANDOFF.md`
- Modify: `docs/handoff/future-plans.md`

- [ ] **Step 1: Run full test suite**

Run: `cd /Users/charliepan/Downloads/my-miniapp && npm test`
Expected: ALL PASS

- [ ] **Step 2: Verify util modules behave correctly via Jest**

Run:
```bash
cd /Users/charliepan/Downloads/my-miniapp && \
  npx jest tests/utils/semaphore.test.js && \
  npx jest tests/utils/producer-consumer.test.js
```

Expected: Both test files pass.

- [ ] **Step 3: Append to `.claude/HANDOFF.md`**

在适当的变更记录区顶部插入同步互斥演示上线记录，包含新增文件清单和变更摘要。

- [ ] **Step 4: Append to `docs/handoff/future-plans.md`**

将「同步互斥演示」从 OS 模块 backlog 中移除（移至已上线记录），保留其余未实现项。

- [ ] **Step 5: Commit**

```bash
git add .claude/HANDOFF.md docs/handoff/future-plans.md
git commit -m "docs(handoff): 同步互斥演示上线记录 + 更新 OS backlog"
```

---

## Task 6: Fix spec gaps (步进粒度 / scroll-view / 阻塞色黄 / 阻塞图标)

**Files:**
- Modify: `pages/sync-viz/sync-viz.js`
- Modify: `pages/sync-viz/sync-viz.wxml`
- Modify: `pages/sync-viz/sync-viz.wxss`

**Spec references:**
- 步进粒度：`docs/superpowers/specs/2026-07-19-sync-viz-design.md` §步进模式
- 缓冲区 scroll-view：同上 §缓冲区渲染
- 阻塞颜色：同上 §操作日志
- 阻塞图标：同上 §阻塞状态

- [ ] **Gap 1: 步进模式改为逐 P/V 操作**

  当前 `_doOneStep` 每步执行完整 `producerStep` 或 `consumerStep`（5 个操作一起）。需改为每次点击只执行一个原子 P/V 操作。

  **改动：**
  - `utils/semaphore.js` 的 `semWait` / `semSignal` 添加到 JS 的 require
  - 新增 `_stepActor` / `_stepSubIdx` / `_stepItem` / `_blockedProducer` / `_blockedConsumer` 跟踪子步骤和阻塞状态
  - 重写 `_doOneStep` 为按 sub-step 执行单个 P/V 操作（支持 sub-step 0-4 分别对应 P(wait) → P(mutex) → produce/consume → V(mutex) → V(signal)）
  - P 操作阻塞时立即切换 actor；5 步完成后自然切换 actor
  - `_syncState` 改用 `_blockedProducer`/`_blockedConsumer` 作为 `waiting` 数据源
  - 自动模式（`_startTimers`）在回调中也维护 `_blockedProducer`/`_blockedConsumer` 状态
  - 新增 `_makeLog` 辅助方法在页面层生成日志条目（ID 从 10000 开始，避免与模块层日志 ID 冲突）

- [ ] **Gap 2: 缓冲区改为 `<scroll-view scroll-x>`**

  当前 `<view class="buffer-grid">` 使用 flex 横向排列。需包裹在 `<scroll-view scroll-x>` 中。

  **改动：** `sync-viz.wxml` 中 buffer-grid 外层加 `<scroll-view scroll-x class="buffer-scroll">`

- [ ] **Gap 3: 阻塞日志用黄色标记**

  当前 `.log-action-blocked` 使用红色系（`#c0392b`）。需改为黄色系暖调。

  **改动：** `sync-viz.wxss` 中修改 `.log-action-blocked` 和 `.log-blocked-badge` 颜色值为暖黄色

- [ ] **Gap 4: 阻塞时箭头变灰 + ⏳ 动画**

  当前阻塞时仅显示文本 banner。需将阻塞中生产/消费者的箭头图标变为灰色并闪烁显示 ⏳。

  **改动：**
  - `sync-viz.wxml`: 箭头处按 `waiting.producer`/`waiting.consumer` 切换为 ⏳ + 灰色样式
  - `sync-viz.wxss`: 新增 `.pointer-arrow-blocked` 样式（灰色 + opacity 脉冲动画）

- [ ] **Step 5: Run test and commit**

  ```bash
  cd /Users/charliepan/Downloads/my-miniapp && npm test
  git add -A && git commit -m "fix(sync-viz): 补齐 spec 差异——逐 P/V 步进/scroll-view/阻塞色黄/阻塞图标"
  ```

---

## 验收清单（实施完成后）

- [ ] `npm test` 全绿
- [ ] `tests/utils/semaphore.test.js` / `tests/utils/producer-consumer.test.js` 全部独立运行通过
- [ ] 微信开发者工具中，`pages/sync-viz/` 加载无报错
- [ ] 默认配置下点击 ▶ → 生产者/消费者自动交替运行，缓冲区逐步填充
- [ ] 缓冲区填满后生产者阻塞（显示阻塞提示），消费者消费后生产者自动恢复
- [ ] 缓冲区清空后消费者阻塞（显示阻塞提示），生产者生产后消费者自动恢复
- [ ] 三个信号量条（full/empty/mutex）数值实时变化，transition 动画流畅
- [ ] 操作日志实时更新，生产者/消费者条目颜色区分
- [ ] 步进模式（⏭）→ 一次 producer 一次 consumer 交替执行
- [ ] 暂停（⏸）→ 所有计时器停止 → 恢复 → 继续运行
- [ ] 重置（↻）→ 缓冲区清空、信号量归位、日志清空
- [ ] 修改缓冲区大小 → 自动重置
- [ ] 修改生产/消费间隔 → 影响运行速度
- [ ] 速度切换（0.5x / 1x / 2x）→ 运行实时调整
- [ ] 首页「操作系统」分类 → 同步互斥演示卡片可点击
- [ ] `.claude/HANDOFF.md` / `future-plans.md` 已更新
